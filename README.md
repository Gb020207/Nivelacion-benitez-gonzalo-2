El trabajo realizado se completo cada implementacion esta en su respectivo archivo 
en la implementacion 1 la forma de ejecutarlo es tan sencilla como presionar 2 veces encima del archivo html y estara funcionando
En la implementacion 2 se pidio que se realice el mismo trabajo pero realizando un servidor basico con node.js primero dentro del archivo en su respectiva terminal (Nivelacion\implementacion2>) ahi lo primero a realizar es npm init -y para dar inicio al proyecto y luego colocar npm i para que instale las dependencias por primeramente se debe ir hacia el package.json y en type normalmente aparece "commonjs" si tratas de ejecutar sin cambiar ese apartado saltara un error asi que se debera cambiar a "module" luego de eso todo debera estar funcionando tambien si es que se desea yo lo hice por comodidad en los scripts podemos cambiar "test" por "dev":"node servidor.js" y a la hora de levantar el servidor el comando que se debe realizar es npm run dev y deberia estar funcionando.
En la implementacion 3 se nos pidio hacer el servidor pero con python es mucho mas sencillo primeramente deberemos tener python instalado (https://www.python.org/downloads/) instalarlo es tan facil como esperar a que se descargue el instalador luego abrimos el mismo y solo presionamos instalar python y cuando nos habra la ventana de coo¿mandos pde python solo colocamos "y", luego dentro del visual si colocamos (py install --configure from the terminal) esto me sirvio para poder ejecutar el comando de python luego de instalar python lo ultimo es ejecutar el comando python servidor.py y ya deberia estar el servidor levantado
